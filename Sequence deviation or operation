#include <stdio.h>
void Array(int n, int start)
{
	int num = 0;
	for (int i = 0; i < n; i++)
	{
		num ^= start + 2 * i;
		
	}
	printf("%d\n", num);
}
int main()
{
	
	Array(5, 0);
	Array(4, 3);
	return 0;
}
